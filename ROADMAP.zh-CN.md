# Mindora 路线图

> [Mindora](https://github.com/Reamory/Mindora) 未来 12 个月的产品规划 —— 一个为 [pi 编程智能体](https://github.com/badlogic/pi-mono)打造的、专注且本地优先的工作空间。

**状态**：草案，欢迎社区评审。分支：`roadmap/v0.2.0`。
**最后更新**：2026-07-09。
**协议**：MIT —— 欢迎贡献。

---

## 一句话总结

Mindora 目前是 `@earendil-works/pi-coding-agent` 的一个**薄薄的浏览器外壳**。v0.1.1 已经发布了品牌、i18n、README 和三语言界面。除此之外的所有东西都是**需要规划的产品工作**，而不只是写代码。

未来 12 个月的四大赌注：

1. **信任与安全** —— 用户必须能放心地在自己的笔记本上运行智能体。今天 Mindora 继承了上游的 `trust-manager.js`，但没有把它暴露出来；我们需要做 *项目信任对话框*、*每次工具调用审批流*、以及*审计日志*。
2. **个人知识层** —— 会话历史本身就是一张隐形的知识图谱。我们将加入*会话搜索*、*项目记忆*，以及一个可选择的*本地 RAG*，跨用户的笔记 / 会话 / git 历史。
3. **同机多智能体** —— pi 编程智能体已经通过扩展点支持子智能体。Mindora 将提供一个*感知 worktree 的任务队列*以及*团队面板*，让用户同时运行一个"代码审查"智能体和一个"实现"智能体。
4. **插件市场** —— 一个 `mindora` 插件格式（TypeScript 文件 + `mindora.plugin.json` 清单），可以注册工具、技能、slash 命令、主题和 UI 面板。通过 npm 的 `mindora-plugin-*` scope 分发。

我们按 6 周一个版本的节奏发布。每个版本聚焦**一个赌注**，外加一些**打磨项**和一项**基础设施**任务。下面是未来 8 个版本，每个版本要发布什么、为什么、以及延后什么。

---

## 目录

1. [背景 —— 我们现在拥有什么、处于什么位置](#1-背景)
2. [原则 —— 我们会做与不会做](#2-原则)
3. [发布计划 —— 12 个月 8 个版本](#3-发布计划)
4. [每个版本的细节 —— 范围、延后项、KPI](#4-每个版本的细节)
5. [横切工作 —— i18n、文档、性能、安全](#5-横切工作)
6. [社区与增长](#6-社区与增长)
7. [2026-2027 不做的事](#7-2026-2027-不做的事)
8. [待回答的问题](#8-待回答的问题)
9. [如何影响这份路线图](#9-如何影响这份路线图)

---

## 1. 背景

### Mindora 现在是什么（v0.1.1）

- 一个 Next.js 16 + React 19 的网页 UI。
- 通过自研 RPC 层（`lib/rpc-manager.ts`）与 `@earendil-works/pi-coding-agent`（也就是跑在 pi CLI 里的同一个智能体）对话。
- 会话文件存在 `~/.pi/agent/sessions/` 下的 `.jsonl` 文件里。
- 主题：浅色 / 深色 / 跟随系统。
- 语言：zh-CN、en-US、ja-JP。
- 品牌：Mindora（Aurora 紫罗兰色，自创 logo，自创 favicon）。

### 哪些能力来自上游，哪些是 Mindora 自己的

| 能力 | pi-coding-agent 上游 | Mindora（今天） | 备注 |
|---|---|---|---|
| 单会话聊天 | ✅ | ✅ | 已有 |
| 会话分支 / fork | ✅（jsonl 条目带 parent_id） | ✅ | 已有 |
| 工具白名单预设（none / default / full） | ✅（BUILTIN_TOOL_NAMES） | ✅ | UI 已提供选择器 |
| `getProjectTrustOptions(cwd)` | ✅（trust-manager.js） | ❌ | **v0.2.0 要把它暴露出来** |
| 扩展运行器 / skills / slash 命令 | ✅ | ✅ | 已集成 |
| 多智能体 / 子智能体 | ⚠️ 部分（扩展 API 存在，没有 UI） | ❌ | v0.4.0 大赌注 |
| MCP（Model Context Protocol）服务器 | ⚠️ 部分 | ❌ | v0.5.0 大赌注 |
| 语音输入 / 多模态输出 | ❌ | ❌ | 也许 v0.7.0 |
| 移动端 App | ❌ | ❌ | 不在路线图 |

### 为什么是这份路线图

用户的本地机器是 AI 编程市场里**最被低估的阵地**。Cursor、Windsurf、Trae、Claude Code、Codex CLI 都在**用户的电脑上**跑，但他们的数据、会话、工具调用对用户来说基本是黑箱 —— 而且他们都向云端靠拢以求"更聪明"的功能。Mindora 的切入点正是：**在一个真正开源的智能体之上，做一个本地优先、透明、可完整审计的 UI**。这份路线图让这个切入点变得具体。

---

## 2. 原则

这些是不可妥协的。每个版本都必须全部满足。

1. **本地优先。** 任何功能都不能要求把数据发到我们控制的服务器。可选的云集成（同步、分享）必须 opt-in、端到端加密、默认关闭。
2. **默认无遥测。** 如果将来加入遥测，必须 opt-in、匿名化，并在 README 中披露。
3. **不自动跑危险工具。** 智能体绝不可以在没有用户明确批准的情况下运行 `bash` / `edit` / `write` / `curl` 或任何被标记为危险的工具 —— 除非用户把项目信任设为"Trust"。信任按目录持久化、可撤销。
4. **保持 MIT。** 本仓库的所有新代码保持 MIT。插件作者可自选协议；我们建议 MIT。
5. **从第一天起就支持 i18n。** 任何版本中加入的用户可见字符串，必须同步存在于 zh-CN、en-US、ja-JP 三种语言。不允许"先英文"的例外。
6. **会话向后兼容。** v0.x 必须能读 v0.y（任何 y ≤ x）的会话。v1.0 将定下长期格式。
7. **默认开放。** 公开的路线图、公开的 RFC、公开的设计文档。决策在 GitHub 上做，不在私信里做。
8. **一个版本一个赌。** 一个版本下两个注会把两个都搞砸。打磨和基础设施可以叠在赌注之上。

---

## 3. 发布计划

| 版本 | 时间 | 赌注 | 头条功能 |
|---|---|---|---|
| **v0.2.0** | 2026-08 | **信任与安全** | 项目信任对话框 + 每次工具审批 + 审计日志 |
| **v0.3.0** | 2026-09 | **知识层 I** | 会话搜索 + 项目记忆 |
| **v0.4.0** | 2026-10 | **多智能体** | 子智能体 UI + worktree 任务队列 |
| **v0.5.0** | 2026-11 | **插件 I** | `mindora` 插件格式 + 市场脚手架 |
| **v0.6.0** | 2026-12 | **知识层 II** | 跨用户笔记 / git / 会话的本地 RAG |
| **v0.7.0** | 2027-01 | **工作流** | 规约驱动模式 + 后台任务队列 |
| **v0.8.0** | 2027-02 | **移动端与 PWA** | 可安装 PWA + 手机友好的侧边栏 |
| **v1.0.0** | 2027-03 | **稳定的会话格式** | 冻结会话 schema + 迁移工具 |

6 周节奏 = 8 个版本约 12 个月。每个版本约 5 周构建 + 1 周 bug 收拾 + 发版。

---

## 4. 每个版本的细节

### v0.2.0 — 信任与安全（2026-08）

**为什么先做这个。** 没有信任，没人会把 Mindora 跑在自己的真实项目上。上游的 `trust-manager.js` 已经存在，但 Mindora 从来不调用它。今天 AI 在 `C:\Users\chiqu\Desktop` 改了一个文件，用户只能等 `git status` 才知道。

**范围（必须发布）**：
- 把 `getProjectTrustOptions(cwd)` 接进项目选择器。
- 新建一个模态对话框："Mindora 想在 `<cwd>` 工作。信任这个文件夹吗？" 提供四个选项（对应上游：Trust / Trust parent / Trust session only / Do not trust）。
- 每次工具调用审批抽屉。当智能体准备调用 `bash` / `edit` / `write` / `curl` 时，显示一张卡片，包含待执行的工具调用 + 文件编辑的 diff 预览。按钮：`Approve` / `Edit & approve` / `Reject`。
- 审计日志面板。每次批准/拒绝的工具调用都被记录。可查看、可搜索、可导出为 JSON。
- 设置 → 安全：按项目列出的信任列表（"`~/Desktop` — 已信任"，"`~/Documents/secrets` — 已拒绝"）。

**延后**：
- 仅本会话的信任（上游提供了，先跳过）。
- bash 命令的风险分类（`rm -rf` 红线等）—— v0.7.0。

**KPI**：
- 当信任为"Do not trust"或未设时，100% 的 `bash` / `edit` / `write` 调用都走审批 UI。
- 审计日志条目可导出，并在会话重载后仍保留。

**风险**：
- 审批 UX 感觉太重。缓解方式：允许一个"完全信任这个项目"开关，一旦打开就不再弹窗，直到用户切换项目。

---

### v0.3.0 — 知识层 I（2026-09）

**为什么第二。** 会话是用户最珍贵的资产。一旦他们信任 Mindora 跑起来，一周就会产生几十个会话。他们需要能找得到。

**范围**：
- Cmd-K 调色板（或 `Ctrl+K`），覆盖会话、分支、文件、slash 命令。
- 会话搜索过滤器：按日期范围、按模型、按触及文件、按项目。
- 项目记忆文件（`~/.pi/agent/projects/<project>/MEMORY.md`）—— 一份策展的长篇笔记，智能体能读但只能用户编辑。可在 UI 中编辑。
- 首页的"近期会话"小部件，按项目分组。
- Star / Pin 会话。被标星的会话在侧边栏的一个分区里浮现。

**延后**：
- 跨会话消息的全文搜索（需要索引 —— v0.6.0）。
- 会话 diff（两个会话并排比较 —— v0.4.0）。

**KPI**：
- Cmd-K < 50 ms 打开。
- "找到我问了 X 的那个会话"在打字后 ≤ 3 次击键就能返回正确会话。

---

### v0.4.0 — 多智能体（2026-10）

**为什么第三。** 一旦用户习惯了一个智能体在一个项目上工作，自然的下一步是"能不能同时跑两个智能体，一个写测试一个写代码？" 上游的 `pi-coding-agent` 已经为子智能体提供了扩展点；缺的就是 UI。

**范围**：
- 顶部栏新增"Agents"面板：列出正在运行的子智能体及其状态（运行中 / 等待 / 已完成）。
- 每个子智能体跑在自己的 git worktree（自动创建）中，所以文件冲突是物理隔离的。
- "生成智能体"按钮：选择角色（"code-reviewer"、"test-writer"、"refactor"）+ 一个目标消息。
- 智能体之间通过共享的"团队聊天"互通 —— 对用户可见。
- 侧边栏里的 worktree 切换器：在 worktree 之间跳转（每个活跃子智能体一个）。

**延后**：
- 跨智能体的冲突解决（自动合并 / rebase —— 需要更多上游工作）。
- 每个智能体的费用上限（cap token 花费）。

**KPI**：
- 在输入一个目标之后，生成一个子智能体只需一次点击。
- 用户能并行跑 3 个以上智能体而 UI 不卡。

**风险**：
- pi-coding-agent 上游的子智能体 API 还在变动。缓解方式：在 v0.4.x 稳定之前，把整个多智能体面板通过一个配置开关 feature-flag 起来。

---

### v0.5.0 — 插件 I（2026-11）

**为什么第四。** 智能体生态是插件驱动的（Claude Code、Codex CLI，都是）。Mindora 需要一个不与上游对抗的插件格式。

**范围**：
- 插件格式：一个导出 `MindoraPlugin` 接口的 TypeScript 文件，外加一个 `mindora.plugin.json` 清单。
- 插件生命周期钩子：`onSessionStart`、`onToolCall`、`onSessionEnd`。
- 插件可以注册：工具、slash 命令、主题、UI 面板（侧边栏 / 状态栏 / 模态框）。
- 发现机制：启动时扫描 `~/.mindora/plugins/` 和 `<project>/.mindora/plugins/`。
- 市场脚手架：`mindora.dev/plugins`（暂用占位 URL）上的一个静态站点社区插件画廊。
- CLI：`mindora plugin install <name>`（从 npm `mindora-plugin-*` scope 解析）。

**延后**：
- 插件沙箱（在 worker / WebAssembly 中运行 —— v1.x）。
- 市场上的评分 / 评论。
- 插件付费。

**KPI**：
- 第三方能按照 `docs/plugins.md` 在 30 分钟内发布一个插件。
- v0.5.0 发布后 30 天内，至少 5 个社区插件。

**风险**：
- v0.5.0 见不到"杀手级插件"。没关系，规则是先把脚手架做好。

---

### v0.6.0 — 知识层 II（2026-12）

**为什么第五。** 用户在 Obsidian、Logseq、纯 Markdown 文件夹，以及 Mindora 会话里都有笔记。我们可以在不向任何云端 embedding 服务发数据的前提下，提供**本地 RAG**。

**范围**：
- 可选的本地 embedding 索引（SQLite + sqlite-vss 或 hnswlib-node）。
- 用户在 设置 → 知识 中选择要索引的文件夹。
- 新 slash 命令 `/recall <query>`：跨已索引文件夹 + 会话返回 top-k 匹配片段。
- "回忆"侧边栏面板，展示最近的 recall。
- 所有 embedding 保留在本地。提供一个"清空"按钮擦除索引。

**延后**：
- 云端 embedding API（OpenAI text-embedding-3 等）—— 用户可以自行配置自己的提供商。
- 向量数据库迁移工具。

**KPI**：
- 在 M 系列 Mac 上，索引 1 万个 Markdown 文件 < 5 分钟。
- `/recall` < 500 ms 返回。

**风险**：
- SQLite + VSS 在 Windows / macOS / Linux 上的可移植性可能有问题。从第一天起就在 Windows 上测（Mindora 的主要开发环境）。

---

### v0.7.0 — 工作流（2027-01）

**为什么第六。** "规约驱动"（spec-driven）是编码智能体圈的流行词（GitHub Spec Kit、Amazon Kiro）。Mindora 应该允许用户写一份规约文件，智能体据此规划 + 实现，并带检查点。

**范围**：
- `/spec <name>` slash 命令：开启一个新会话，其系统提示包含一份规约模板。
- 规约模板：目标 / 范围外 / 验收标准 / 待回答问题。存为 `<project>/.mindora/specs/<name>.md`。
- 在规约模式下，智能体在每个主要步骤之后暂停，请用户批准 / 修改。
- "后台任务"模式：`/run-async` 把任务排入队列，在用户做其它事时运行。
- 审计日志（v0.2.0）成为规约模式下会话的规范历史记录。

**延后**：
- 把一个规约自动分解为子规约（规约图）。
- 时间盒运行（"给这个 10 分钟然后停"）。

---

### v0.8.0 — 移动端与 PWA（2027-02）

**为什么第七。** 用户有时会离开他们的开发机。他们应该能从手机上查看一个正在运行的智能体、批准一个待审批的工具调用、或阅读一个会话。

**范围**：
- PWA manifest + service worker。可从 Chrome / Safari 安装。
- 移动端布局：可折叠侧边栏变为底部抽屉；聊天变为全屏。
- 默认只读模式。编辑 / 发送需要一个明确的"连接"手势（避免误触批准）。
- 当智能体需要批准时推送通知（Web Push API）。

**延后**：
- 原生 iOS / Android App。
- 语音输入（移动端很合适，但语音 UX 是独立的设计问题 —— 延后到 v0.9+）。

---

### v1.0.0 — 稳定的会话格式（2027-03）

**为什么是最后一个，却最重要。** 8 个版本的迭代之后，我们锁定：
- 会话 `.jsonl` schema（向前兼容：忽略未知字段，而不是拒绝）。
- 插件清单 schema（带语义化版本）。
- API 表面（`lib/rpc-manager.ts` 升至 `1.0`）。
- 迁移工具：`mindora migrate-sessions <from> <to>` 处理 v0.x 之间的所有破坏性变更。

**范围**：
- 为 `SessionEntry`、`SessionTreeNode`、`ToolEntry`、`PluginManifest` 冻结 schema。
- 迁移 CLI。
- 创建 `LTS` 分支。关键修复 backport 6 个月。
- 文档化废弃策略：被废弃的 API 至少存活 2 个 minor 版本再删除。

---

## 5. 横切工作

**每个版本都必须发布**的东西：

| 关注点 | 负责人 | 我们一直做什么 |
|---|---|---|
| **i18n** | 待定 | 任何新的 UI 字符串必须同步加到 `lib/i18n/dictionary.ts` 的所有 3 种语言中。缺这个的 PR 会被拦下。 |
| **无障碍** | 待定 | 每个新的交互元素必须支持键盘导航，并有 `aria-label`。与 `--bg` 的对比度 ≥ 4.5:1。 |
| **测试** | 待定 | `lib/*.test.mjs` 测逻辑；Playwright smoke 测 UI 关键路径（发消息、打开会话、切换项目）。 |
| **文档** | 待定 | 更新 CHANGELOG.md。新功能在 README 或 `docs/` 中写明。 |
| **安全** | 待定 | `app/api/` 中的新端点要有输入校验 + 限流。 |
| **性能** | 待定 | 首屏 JS 预算 ≤ 250 KB。每个版本手动测试 SSE 重连。 |

---

## 6. 社区与增长

Mindora 是一个开源项目。如果没人用，路线图毫无意义。

### 我们会投入的渠道

| 渠道 | 频率 | 负责人 |
|---|---|---|
| GitHub Discussions | 每日检查 | 维护者 |
| GitHub Issues | 每日分诊 | 维护者 |
| Discord（链接待定） | 每周 office hour | 维护者 + 社区 |
| 季度发布笔记（博客 / `CHANGELOG.md`） | 每个版本 | 维护者 |

### 增长杠杆（按顺序）

1. **README 与文档的质量** —— 人们不采用一个 OSS 工具的首要原因是装不上。我们会严格保持 `README.md` 与时俱进。
2. **README 里放一个 demo GIF**。展示信任对话框在首次运行时弹出，用户点 Approve，智能体写一个文件，文件出现在文件预览中。30 秒，胜过 1000 字。
3. **插件作者就是布道者。** 当有人发布了一个 `mindora-plugin-*` 而且做得不错，我们在市场上置顶推荐。
4. **Show, don't tell.** 每个版本都配一段能跑的 demo 录制。Twitter / X / 哔哩哔哩 / V2EX / Hacker News 分享的是录制，不是营销文案。
5. **Good first issues** —— 每个版本挑 3-5 个 issue 打上 `good-first-issue` 标签，欢迎新人。

### 我们不会做的

- 赞助帖 / 付费获客。（我们是一人项目，预算是工时。）
- Discord 独占公告。（GitHub 才是真相之源。）
- Mindora 内部的订阅 / 付费层。

---

## 7. 2026-2027 不做的事

这些都很诱人。不在路线图里。我们会在 v1.0.0 之后重新评估。

- **云同步 / "Mindora Cloud"。** 与本地优先原则冲突。用户想要同步就用 `git`、`Syncthing` 或 `iCloud`。
- **语音输入。** 有用，但语音 UX 是一个独立的设计领域。等 v0.9+。
- **原生桌面 App**（Tauri / Electron 包装）。PWA 已经覆盖 90% 用例，成本只有 10%。
- **带付费的市场。** 等插件量足以支撑平台再做。
- **代码执行沙箱**（在 Docker 容器中跑智能体）。违背本地优先原则。信任对话框就是答案。
- **私有模型层。** Mindora 只发客户端；模型选择是用户的事，推理也是用户的事。
- **替换 pi 编程智能体。** Mindora 是 `@earendil-works/pi-coding-agent` 之上的 UI。如果我们偏离，那就是 fork，不是路线图项。

---

## 8. 待回答的问题

这些问题需要在 v0.4.0 发布前得到答案。打 `roadmap-question` 标签的 issue。

1. **"Trust" 应该是按项目、按文件夹，还是两者都有？** 上游支持按文件夹。我们可能想要一个项目级信任，能覆盖用户最常用项目的文件夹信任。
2. **插件权限模型。** 当一个插件注册一个工具，它拿到什么权限？和内置工具一样，还是沙箱化？（我们倾向：默认一样，插件通过清单 opt-in 沙箱化。）
3. **多智能体的费用上限。** 我们要不要硬性 cap 每个智能体会话的 token 花费？如果要，cap 放在哪里 —— 智能体配置、用户配置、还是每次生成时指定？
4. **规约驱动模式的 UX。** 暂停并审批太重。能不能有一个更轻的"信任但定期检查"模式？
5. **迁移故事。** 当 v1.0 冻结会话 schema 时，未来某个使用实验性字段的 v0.x 写出的会话会怎样？忽略未知字段（宽松），还是拒绝该文件（严格）？

---

## 9. 如何影响这份路线图

1. **提一个 issue**，打上 `roadmap` 标签。引用你想改动的版本。
2. **对这个文件开一个 PR**，如果你有具体的改动。
3. **评论 `docs/rfcs/` 下的公开 RFC**。
4. **用 Star 投票** —— GitHub star 数是我们拥有的最诚实的信号。如果某个版本很受欢迎，我们会据此调整下个版本的路线图。

---

## 附录 A — 规划分支的文件结构

```text
ROADMAP.md                      ← 本文件
ROADMAP.zh-CN.md                ← 中文版
docs/
  logo.svg
  plugins.md                    （v0.5.0 时写）
  rfcs/
    0001-trust-dialog.md        （v0.2.0）
    0002-tool-approval.md       （v0.2.0）
    0003-subagent-ui.md         （v0.4.0）
    0004-plugin-format.md       （v0.5.0）
    0005-local-rag.md           （v0.6.0）
    0006-spec-driven.md         （v0.7.0）
```

## 附录 B — 灵感与参考

- [pi-mono](https://github.com/badlogic/pi-mono) —— 为 Mindora 提供动力的智能体。
- [Claude Code](https://docs.claude.com/claude-code) —— slash command + skills UX 的黄金标准。
- [Cursor](https://www.cursor.com) —— Composer / 多文件编辑模型。
- [Windsurf](https://codeium.com/windsurf) —— "Flow" 状态概念。
- [GitHub Spec Kit](https://github.com/github/spec-kit) —— 规约驱动开发。
- [Model Context Protocol](https://modelcontextprotocol.io) —— 智能体工具的 USB-C。
- [Obsidian](https://obsidian.md) 和 [Logseq](https://logseq.com) —— 本地优先笔记的灵感。
- [SQLite + VSS](https://github.com/asg017/sqlite-vss) —— 本地向量搜索。
- [Contributor Covenant](https://www.contributor-covenant.org) —— Code of Conduct 模板。

---

*由 Remory 与 Mindora 社区用心打造。*